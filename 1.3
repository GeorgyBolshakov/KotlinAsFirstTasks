import kotlin.math.*
fun angleInRadian(grad: Int, min: Int, sec: Int): Double {
    val a = grad * PI / 180
    val b = min * PI / (180 * 60)
    val c = sec * PI / (180 * 3600)
    return a + b + c
}
fun main() {
    println(angleInRadian(36, 14, 35))
}
